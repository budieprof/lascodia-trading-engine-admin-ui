import { Routes } from '@angular/router';
import { ConfigPageComponent } from './pages/config-page/config-page.component';

export const ENGINE_CONFIG_ROUTES: Routes = [{ path: '', component: ConfigPageComponent }];
