import { Routes } from '@angular/router';
import { EnsemblePageComponent } from './pages/ensemble-page/ensemble-page.component';

export const STRATEGY_ENSEMBLE_ROUTES: Routes = [{ path: '', component: EnsemblePageComponent }];
