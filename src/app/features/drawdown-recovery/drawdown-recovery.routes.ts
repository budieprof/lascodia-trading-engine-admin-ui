import { Routes } from '@angular/router';
import { DrawdownPageComponent } from './pages/drawdown-page/drawdown-page.component';

export const DRAWDOWN_RECOVERY_ROUTES: Routes = [
  { path: '', component: DrawdownPageComponent },
];
